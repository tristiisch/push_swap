PUSH_SWAP >

make
Compile les programmes push_swap & checker

./push_swap $ARG -h -i
Affiche le nombre d'instructions pour ranger la list de $ARG

time ./push_swap $ARG -h
Affiche le temps que prends l'algo

ARG=$(./randomize 500); ./push_swap $ARG -h -i

RANDOMIZE >

make randomize
Compile le programme randomize

ARG=$(./randomize 500)
Met 500 nombre aléatoire unique dans ARG


TESTER >
ARG=$(./randomize 500); ./push_swap $ARG -h -i

ARG=$(./randomize 100); ./push_swap $ARG | ./checker $ARG

TESTER LES ALGO AVEC BAREME >

ARG=$(./randomize 10); ./push_swap $ARG -t
ARG=$(./randomize 100); ./push_swap $ARG -t
ARG=$(./randomize 500); ./push_swap $ARG -t